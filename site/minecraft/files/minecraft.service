[Unit]
Description=Minecraft Server

Wants=network.target
After=network.target

[Service]
WorkingDirectory=/opt/minecrat
ExecStart=/usr/bin/java -Xmx512M -Xms32M -jar server.jar nogui

[Install]
WantedBy=multi-user.target
